# Docker Compose Environment Variables
# Copy this file to .env (in root directory) and configure for your environment
# These variables are used by docker-compose.yml

# SECURITY WARNING: Change these default passwords in production!
# The defaults below are ONLY for local development

# PostgreSQL Configuration
POSTGRES_USER=postgres
# SECURITY: Change this password for production deployments
POSTGRES_PASSWORD=change_this_in_production_db_pass_2024
POSTGRES_DB=viabaq_db

# TimescaleDB Configuration
TIMESCALE_USER=postgres
# SECURITY: Change this password for production deployments
TIMESCALE_PASSWORD=change_this_in_production_ts_pass_2024
TIMESCALE_DB=viabaq_timeseries

# pgAdmin Configuration
PGADMIN_EMAIL=admin@viabaq.com
# SECURITY: Change this password for production deployments
PGADMIN_PASSWORD=change_this_in_production_admin_pass_2024

# NOTES:
# - For local development, you can use the default fallback values in docker-compose.yml
# - For production, ALWAYS set these variables in your deployment environment
# - Never commit real passwords to version control
# - Use strong, unique passwords for each service
